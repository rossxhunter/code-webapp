<%= form_with(model: track_template, local: true) do |form| %>
  <% if track_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(track_template.errors.count, "error") %> prohibited this track_template from being saved:</h2>

      <ul>
      <% track_template.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :order %>
    <%= form.number_field :order %>
  </div>

  <div class="field">
    <%= form.label :course_id %>
    <%= form.number_field :course_id %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
