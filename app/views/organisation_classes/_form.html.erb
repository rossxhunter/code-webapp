<%= form_with(model: organisation_class, local: true) do |form| %>
  <% if organisation_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(organisation_class.errors.count, "error") %> prohibited this organisation_class from being saved:</h2>

      <ul>
      <% organisation_class.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row">
    <%= form.label :name%>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <div class="form-group row">
    <%= form.label :code%>
    <%= form.text_field :code, class: "form-control" %>
  </div>

  <%= form.hidden_field :organisation_id, value: current_teacher.organisation_id %>

  <div class="actions row">
    <%= form.submit 'Create Class', class: "btn btn-primary" %>
  </div>
<% end %>
